version: 0.2

phases:
  pre_build:
    commands:
      - yarn install
      - yarn lint
  build:
    commands:
      - yarn build
cache:
  paths:
    - 'node_modules/**/*'
artifacts:
  files:
    - '**/*'
  base-path: 'dist/apps'
  secondary-artifacts:
    api:
      files:
        - '**/*'
      base-path: 'dist/apps/api'
      name: lets-choose-api
    client:
      files:
        - '**/*'
      base-path: 'dist/apps/client'
      name: lets-choose-client
